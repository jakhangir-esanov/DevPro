@using ProjectManagementSystem.Service.DTOs.Teams;
@model IEnumerable<TeamResultDto>;

<div class="row">
    <div class="col-11">
        <h1 class="mb-3 mt-2">Teams</h1>
    </div>
    <div class="col-1 mt-4">
        <p><a class="btn btn-primary" asp-controller="Team" asp-action="Create" style="background-color: green">Create</a></p>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            @{
                var id = Model.FirstOrDefault();
            }
            <th>@Html.DisplayNameFor(item => id.Id)</th>
            <th>@Html.DisplayNameFor(item => id.Name)</th>
            <th>@Html.DisplayNameFor(item => id.Description)</th>
            <th>Edit</th>
            <th>Members</th>
            <th>Projects</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>
                    @Html.DisplayFor(i => item.Id)
                </th>
                <th>
                    @Html.DisplayFor(i => item.Name)
                </th>
                <th>
                    @Html.DisplayFor(i => item.Description)
                </th>
                <th>
                    <p><a class="btn btn-primary" asp-controller="Team" asp-action="Edit" asp-route-id="@item.Id">Edit</a></p>
                </th>
                <th>
                    <form asp-controller="Team" asp-action="Detail" asp-route-id="@item.Id">
                        <button class="btn btn-success btn-rounded mb-3" style="background-color: orange">Members</button>
                    </form>
                </th>
                <th>
                    <p><a class="btn btn-primary" asp-controller="Team" asp-action="Project" asp-route-id="@item.Id" style="background-color: green">Projects</a></p>
                </th>
                <th>
                    <form asp-controller="Team" asp-action="Delete" asp-route-id="@item.Id">
                        <button class="btn btn-success btn-rounded mb-3" style="background-color: red">Delete</button>
                    </form>
                </th>
            </tr>
        }
    </tbody>
</table>